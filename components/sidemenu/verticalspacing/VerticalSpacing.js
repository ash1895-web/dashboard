import styles from './VerticalSpacing.module.css'

const VerticalSpacing = ({height}) => {
    return(
        <div className={styles.verticalSpacingContainer} style={{height}}>

        </div>
    )
}

export default VerticalSpacing