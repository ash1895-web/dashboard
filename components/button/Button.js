import styles from './Button.module.css'

const Button = () => {
    return (
        <div className = {styles.buttonContainer}>
            apple eating disorder
        </div>
    )
}

export default Button